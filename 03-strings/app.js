// Veamos los METODOS en concreto del tipo STRING
// Un METODO es una función propia de un objeto, en este caso OBJETO = VARIABLE STRING

const who = "Batman";
console.log(who);


// Interpolar variables con comilla francesa o invertida
// expresión para inyectar ` ... ${ INTERPOLACION } `

console.log(`Yo soy ${who}`);
